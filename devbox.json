{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.14.2/.schema/devbox.schema.json",
  "packages": [
    "python@3.13",
    "poetry@latest",
    "mdformat@latest"
  ],
  "shell": {
    "init_hook": [
      ". $VENV_DIR/bin/activate",
      "poetry install --no-interaction"
    ],
    "scripts": {
      "test": [
        "echo \"Error: no test specified\" && exit 1"
      ],
      "mypy": [
        "mypy ${@:-.}"
      ],
      "black": [
        "black ${@:-.}"
      ],
      "isort": [
        "isort --skip .venv --skip .devbox ${@:-.}"
      ],
      "mdformat": [
        "mdformat ${@:-.}"
      ],
      "run": [
        "poetry run python pool_collector/main.py"
      ]
    }
  }
}
